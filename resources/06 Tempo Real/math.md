amplitude * np.exp(-decay * x) * np.cos(phase + 2 * np.pi * frequency * x / N)




$$
a e^{-dx} \cos(\phi + 2 \pi f x / n)
$$

$$
X = \{1,...,n\} \\
A = \{ a_1, ..., a_n \} \\
F = \{ f_1, ..., f_n \}
$$